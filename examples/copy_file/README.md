# copy_file Demo

The copy_file demo demonstrates how to copy a script file onto the MiniNet image.

## Running the Demo

To run the demo network, type `./net/run.py`.

In the demo shell, type `sh /tmp/myscript.py`. You should see the output "It works!".
